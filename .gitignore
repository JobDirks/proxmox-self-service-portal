# Build and IDE
bin/
obj/
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# Logs and temp
*.log
logs/
*.trace
*.tmp

# Secrets and local config
secrets.json
appsettings.*.local.json

# Local environment & user secrets
.env
*.user

# SQLite and data
*.db
*.sqlite

# Root data folder (SQLite DB etc.)
/data/
/vmportal.db

# If you want to keep the data folder structure in repo, create /data/.gitkeep
# and then:
!/data/.gitkeep

# Old Web data folder (kept ignored for safety, even if unused now)
/VmPortal.Web/data/
/VmPortal.Web/data/*.db
!/VmPortal.Web/data/.gitkeep

# Node artifacts (if added later)
node_modules/
dist/